production:
  solr:
    hostname: localhost
    port: 8983
    log_level: WARNING

development:
    solr:
        hostname: localhost
        port: 8983
        log_level: INFO
        #path: /Applications/Development/rails-apps/nicher_documents/indexedassets
        # pid_dir: defaults to 'app/solr/pids/development'
        # bind_address: defaults to '0.0.0.0' which binds to every interface
        disabled: false # note that this is not indented within `solr`
#    
#  solr:
#    hostname: localhost
#    port: 8982
#    log_level: INFO

test:
  solr:
    hostname: localhost
    port: 8981
    log_level: WARNING